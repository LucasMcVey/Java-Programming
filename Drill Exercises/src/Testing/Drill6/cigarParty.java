package Testing.Drill6;

public class cigarParty {
    public boolean cigarParty(int cigars, boolean isWeekend) {


        if(isWeekend = false)
            if (cigars > 40)
                if (cigars < 60)
                {
                    return isWeekend = true;
                }
        else return isWeekend = false;

        if(isWeekend = true)
            if (cigars < 40)
            {
                return isWeekend = false;
            }
                if (cigars > 40)
                {
                    return isWeekend = true;
                }
                if (cigars > 60)
                {
                return isWeekend = true;
                }
        else return isWeekend = false;



    }

}





